var angular = require('angular'); // dołączenie angulara
var app = angular.module('spotify-search', []); // inicjalizacja głównego modułu angulara

require('./app/components/main/main.component'); // dodanie głównego komponentu
